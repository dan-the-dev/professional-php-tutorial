{% extends 'Layout.html.twig' %}

{% block content %}
    {% include'FlashMessages.html.twig' %}
    <ol>
        {% for submission in submissions %}
            <li>
                <a href="{{ submission.url }}">{{ submission.title }}</a><br>
                <small>submitted by {{ submission.author }}</small>
            </li>
        {% endfor %}
    </ol>
{%  endblock %}